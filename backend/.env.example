APP_NAME=Temu Scraper Backend
APP_ENV=development
DEBUG=true

# 本地开发数据库（127.0.0.1）
DATABASE_URL=postgresql+psycopg://root:root@127.0.0.1:5432/temu_scraper

# JWT 密钥（本地随意填，生产必须换成随机长字符串）
JWT_SECRET=local-dev-secret-change-in-production
JWT_EXPIRE_MINUTES=10080
